we first find the index of the element having minimum element in the row using a utility functions
and then again iterate over all the rows to check if the element is maximum over all the column
if yes then we append to target vector res and at last we return the res